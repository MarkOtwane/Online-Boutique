import { TrackingManagementComponent } from './tracking-management.component'

describe('TrackingManagementComponent', () => {
  it('should mount', () => {
    cy.mount(TrackingManagementComponent)
  })
})